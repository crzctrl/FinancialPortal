@*@using FinancialPortal.Models
@using FinancialPortal.Helpers
@using Microsoft.AspNet.Identity*@
@model IEnumerable<FinancialPortal.Models.ApplicationUser>
@{
    ViewBag.Title = "Members";
    //var db = new ApplicationDbContext();
    //var rHelp = new RoleHelper();
    //var user = db.Users.Find(User.Identity.GetUserId());
}

<div class="page-header">
    <div class="page-block">
        <div class="row align-items-center">
            <div class="col-md-12">
                <div class="page-header-title">
                    <h5 class="m-b-10">The Household</h5>
                </div>
                <ul class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Dashboard", "Home")"><i class="feather icon-home"></i></a></li>
                    <li class="breadcrumb-item">Family Tree</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">
        <div class="card card-border-c-blue">
            <div class="card-header">
                <h2>Members</h2>
            </div>
            <div class="card-body">
                <div class="dt-responsive table-responsive">
                    <table class="table table-striped table-bordered nowrap">
                        <thead>
                            <tr>
                                <th></th>
                                <th>
                                    @Html.DisplayName("First Name")
                                </th>
                                <th>
                                    @Html.DisplayName("Last Name")
                                </th>
                                <th>
                                    @Html.DisplayName("Display Name")
                                </th>
                                <th>
                                    @Html.DisplayName("Email")
                                </th>
                                @*<th>
            @Html.DisplayName("Role")
        </th>
        <th></th>*@
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">
                                        <img src="@item.AvatarPath" style="width: 100px" />
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.FirstName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.LastName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DisplayName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    @*<td>
                                        @Html.DisplayFor(modelItem => item.Roles.Where(r => r.UserId == item.Id).FirstOrDefault().RoleId)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                                        @if (User.IsInRole("Head of Household"))
                                        {
                                            <p>kick a member here :)</p>
                                        }
                                    </td>*@
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>